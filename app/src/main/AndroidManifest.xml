<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.dsergio.android.test"
    android:versionCode="1"
    android:versionName="1.0" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity android:name=".activity.FirstAccessActivity" />
        <activity android:name=".activity.MenuActivity" />
        <activity android:name=".activity.NewDataActivity"/>
        <activity android:name=".activity.InputDataActivity"/>
        <activity android:name=".activity.LocalDataActivity"/>
        <activity android:name=".activity.RemoteDataActivity"/>
        <activity
            android:name=".activity.SplashActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.LoginActivity"
            android:label="LoginActivity" >
            <intent-filter>
                <action android:name="it.dsergio.android.test.action.LOGIN_ACTION" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.RegisterActivity"
            android:label="@string/title_activity_register" >
            <intent-filter>
                <action android:name="it.dsergio.android.test.action.REGISTRATION_ACTION" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.ShowUserDataActivity"
            android:label="@string/register_activity_label">
        <intent-filter>
            <action android:name="it.dsergio.android.test.action.SHOW_USER_ACTION"/>
            <category android:name="android.intent.category.DEFAULT"/>
        </intent-filter>
        </activity>
    </application>

</manifest>
